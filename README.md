The application has a slash screen that I created in MainActivity the splash screen will transtion to the ActvityMain after 3 seconds  . In MainActivity 2 ,when the app opens ,the user is met with the following sequence describing what happenns during interaction
Entering infromation :The user fills in four input fields (Tent,Shelter,Quantity,4-person waterproof)
Selecting Add Gear :Once c;icked ,the app immediately performs error chceking:
It verifes that one of the fields are empty
It confirms that the quantity entered is valid number geather than zero
If any issues id detected ,the process stops and a Toast messagge alters the user. If all inputs are valid ,the app checks the current conuter value(itemCount),because thisis the first entry the index is 0 .The four pirces of information are then stored in 0 of their lists.When the user clicks NEXT the app creates an intent to open the displayed screen .
When the display screen loads , the user sees empty results area along with  two action buttons.The screen relies on loops to read through the list shared in MainActivity 2.The app begins a for loop that instructs the system to start index 0 and coontinue up to the current value of the item counter fro each index,it retrieves the name ,category,quantity,and comment and combines them into a formatted textblock,and appends that block to the screens text view.The app executes the finish() command,which closes the second screenand returns the user to MainActivity 2.All previously saved items remain unchanged and available.
